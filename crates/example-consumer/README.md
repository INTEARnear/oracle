# An example of how to use the oracle contract as a consumer from your smart contract

This contract just calls `request` on the oracle contract, and then processes the response in a private callback.

